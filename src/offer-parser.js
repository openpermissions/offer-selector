function parseOffer(){
  /*
  return new Promise(function (resolve,reject) {
    if(reject) { console.log("ew");}
    else if(resolve){
      console.log("here");
      offerObj.forEach(i => {
        const type = i['@type'];
        console.log(type);

      })
     }
   })
   */
   return true;
  }
